{% extends 'base.html.twig' %}

{% block body %}
    <div class="flex items-center justify-center min-h-screen bg-gray-100">
        <div class="bg-white shadow-lg rounded-lg p-8 max-w-md text-center">
            <h1 class="text-4xl font-bold text-red-600 mb-4">403</h1>
            <h2 class="text-2xl font-semibold text-gray-800 mb-2">Access Denied</h2>
            <p class="text-gray-600 mb-6">You do not have permission to access this page.</p>

            <a href="{{ path('homepage') }}" class="px-6 py-2 text-white bg-blue-600 rounded-lg hover:bg-blue-700 transition">
                Go back to Home
            </a>
        </div>
    </div>
{% endblock %}
